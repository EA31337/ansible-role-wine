---
- name: Ensures Wine is present
  become: true
  become_method: ansible.builtin.su
  block:

    - name: Ensures Wine is present ({{ ansible_facts["os_family"] }})
      ansible.builtin.package:
        name: "wine{% if wine_version %}={{ wine_version }}{% endif %}"
        state: present
        update_cache: true
