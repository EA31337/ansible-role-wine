---
# Molecule configuration file.
# Dependency management (download roles/collections)
# Note: Keep YAML keys sorted alphabetically.
dependency:
  name: galaxy
  options:
    ignore-certs: false
    ignore-errors: false
    requirements-file: requirements.yml
    role-file: requirements.yml
driver:
  name: docker
lint: |
  set -e
  yamllint .
  molecule syntax
  ansible-lint
# Platform names follow the `<role>-<scenario>-<platform>` convention so the
# Docker containers (named after the platform) stay unique across roles and
# scenarios. Molecule's Docker driver names each container exactly after its
# platform, so generic names such as `debian-latest` collide with concurrent
# Molecule runs of other roles or scenarios.
platforms:
  - name: wine-default-alpine-latest
    dockerfile: ../resources/playbooks/Dockerfile.j2
    image: alpine:3.20
    init: true
    pre_build_image: false
  - name: wine-default-debian-latest
    image: debian:latest
    init: true
  - name: wine-default-nixos-latest
    dockerfile: ../resources/playbooks/Dockerfile.j2
    image: nixos/nix:latest
    init: true
    pre_build_image: false
    privileged: true
  - name: wine-default-ubuntu-jammy
    image: ubuntu:jammy
    init: true
  - name: wine-default-ubuntu-noble
    image: ubuntu:noble
    init: true
provisioner:
  config_options:
    defaults:
      # Workaround for molecule-docker 2.1.0 using non-boolean conditionals
      # in its create/destroy playbooks (e.g. `when: (lookup('env', 'HOME'))`).
      # See: https://github.com/ansible-community/molecule-plugins/issues/295
      allow_broken_conditionals: true
      callbacks_enabled: ansible.posix.profile_tasks
      # Ensure collections installed to ./collections are found by ansible-playbook.
      collections_path: ${MOLECULE_PROJECT_DIRECTORY}/collections:~/.ansible/collections:/usr/share/ansible/collections
      display_ok_hosts: false
      display_skipped_hosts: false
      host_key_checking: false
      # Use the condensed unixy callback.
      stdout_callback: community.general.unixy
      verbosity: 0
  env:
    ANSIBLE_FORCE_COLOR: "1"
    ANSIBLE_LOAD_CALLBACK_PLUGINS: "1"
  inventory:
    group_vars:
      all:
        wine_init: true
        wine_no_log: "{{ ansible_verbosity | default(0) < 1 }}"
        wine_release: stable
    host_vars:
      wine-default-debian-latest:
        wine_release_codename: bookworm
        wine_version: "10.0"
      wine-default-nixos-latest:
        ansible_python_interpreter: /root/.nix-profile/bin/python3
        wine_version: "10.0"
      wine-default-ubuntu-jammy:
        wine_release_codename: jammy
        wine_version: "10.0"
      wine-default-ubuntu-noble:
        wine_release_codename: noble
        wine_version: "10.0"
  name: ansible
  options:
    diff: true
    force-handlers: true
  playbooks:
    converge: converge.yml
    create: create.yml
    destroy: destroy.yml
    prepare: prepare.yml
    verify: verify.yml
  ssh_connection:
    pipelining: true

scenario:
  name: default
  test_sequence:
    - dependency
    - destroy
    - syntax
    - create
    - prepare
    - converge
    - idempotence
    - verify
    - destroy
verifier:
  name: ansible
