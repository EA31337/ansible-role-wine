---
- name: Prepare hosts
  hosts: "all:!nixos*"
  gather_facts: false
  tasks:

    - name: Ensure sudo is installed
      ansible.builtin.raw: |
        if command -v sudo >/dev/null 2>&1; then
          exit 0
        elif command -v apk >/dev/null 2>&1; then
          apk add --no-cache sudo
        elif command -v apt-get >/dev/null 2>&1; then
          if [ -f /etc/os-release ] && grep -qi '^ID=ubuntu' /etc/os-release; then
            sed -i -e 's|http://archive.ubuntu.com/ubuntu|http://azure.archive.ubuntu.com/ubuntu|g' \
              -e 's|http://security.ubuntu.com/ubuntu|http://azure.archive.ubuntu.com/ubuntu|g' /etc/apt/sources.list
          fi
          apt-get update -qq && DEBIAN_FRONTEND=noninteractive apt-get install -y -q sudo
        elif command -v dnf >/dev/null 2>&1; then
          dnf install -y sudo
        elif command -v zypper >/dev/null 2>&1; then
          zypper --non-interactive install sudo
        else
          echo "No supported package manager found to install sudo" >&2
          exit 1
        fi
      changed_when: false

    - name: Trust proxy CA certificates (if mounted)
      ansible.builtin.raw: |
        if ls /usr/local/share/ca-certificates/*.crt >/dev/null 2>&1; then
          if command -v apk >/dev/null 2>&1; then
            apk add --no-cache ca-certificates
            cp /usr/local/share/ca-certificates/*.crt /usr/share/ca-certificates/
            update-ca-certificates
          elif command -v apt-get >/dev/null 2>&1; then
            apt-get update -qq && apt-get install -y -q ca-certificates
            cp /usr/local/share/ca-certificates/*.crt /usr/share/ca-certificates/
            for f in /usr/local/share/ca-certificates/*.crt; do
              basename "$f" >> /etc/ca-certificates.conf
            done
            update-ca-certificates
            chmod 755 /etc/ssl/certs
          elif command -v update-ca-certificates >/dev/null 2>&1; then
            update-ca-certificates
          fi
        fi
      changed_when: false

    - name: Install Python for Ansible
      ansible.builtin.raw: |
        if command -v python3 >/dev/null 2>&1 || command -v python >/dev/null 2>&1; then
          exit 0
        elif command -v apk >/dev/null 2>&1; then
          apk add --no-cache python3 py3-pip
        elif command -v apt-get >/dev/null 2>&1; then
          if [ -f /etc/os-release ] && grep -qi '^ID=ubuntu' /etc/os-release; then
            sed -i -e 's|http://archive.ubuntu.com/ubuntu|http://azure.archive.ubuntu.com/ubuntu|g' \
              -e 's|http://security.ubuntu.com/ubuntu|http://azure.archive.ubuntu.com/ubuntu|g' /etc/apt/sources.list
          fi
          apt-get update -qq && DEBIAN_FRONTEND=noninteractive apt-get install -y -q python3-minimal
        elif command -v dnf >/dev/null 2>&1; then
          dnf install -y python3
        elif command -v zypper >/dev/null 2>&1; then
          zypper --non-interactive install python3
        else
          echo "No supported package manager found" >&2
          exit 1
        fi
      changed_when: false

- name: Prepare NixOS hosts
  hosts: "nixos*"
  gather_facts: false
  tasks:

    - name: Install Python for Ansible on NixOS
      ansible.builtin.raw: |
        if command -v python3 >/dev/null 2>&1 || command -v python >/dev/null 2>&1; then
          exit 0
        fi
        mkdir -p /etc/nix
        touch /etc/nix/nix.conf
        if grep -q '^sandbox =' /etc/nix/nix.conf; then
          sed -i 's/^sandbox =.*/sandbox = false/' /etc/nix/nix.conf
        else
          printf "sandbox = false\n" >> /etc/nix/nix.conf
        fi
        export NIX_CONFIG="sandbox = false"
        nix-env --option sandbox false \
          -iA nixpkgs.python3 nixpkgs.python3Packages.pip
      changed_when: false

    - name: Trust proxy CA certificates on NixOS (if mounted)
      ansible.builtin.raw: |
        if ls /usr/local/share/ca-certificates/*.crt >/dev/null 2>&1; then
          combined="/etc/nix/ca-bundle.crt"
          mkdir -p /etc/nix
          cp /nix/var/nix/profiles/default/etc/ssl/certs/ca-bundle.crt "$combined"
          cat /usr/local/share/ca-certificates/*.crt >> "$combined"
          if ! grep -q 'ssl-cert-file' /etc/nix/nix.conf 2>/dev/null; then
            printf "ssl-cert-file = %s\n" "$combined" >> /etc/nix/nix.conf
          fi
        fi
      changed_when: false
